{
  "name": "website-chatbot",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "echo 'Use npx vercel dev to start development server'",
    "build": "echo 'No build step required'",
    "deploy": "vercel --prod",
    "start": "echo 'Use npx vercel dev to start development server'"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.45.4",
    "node-fetch": "^3.3.2"
  },
  "devDependencies": {
    "@vercel/node": "^4.0.0"
  },
  "engines": {
    "node": "22.x"
  }
}
